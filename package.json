{
  "name": "tabhub-spotify-dashboard",
  "private": true,
  "version": "0.1.0",
  "type": "module",
  "scripts": {
    "dev": "vite",
    "build": "tsc && vite build && node -e \"require('fs').writeFileSync('dist/version.json', JSON.stringify({version: Date.now()}))\"",
    "preview": "vite preview"
  },
  "devDependencies": {
    "@vitejs/plugin-basic-ssl": "^2.3.0",
    "typescript": "^5.5.3",
    "vite": "^5.3.4"
  }
}
